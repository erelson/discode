// Re-export from canonical location for backward compatibility
export {
  CLAUDE_PLUGIN_NAME,
  getClaudePluginDir,
  getPluginSourceDir,
  installClaudePlugin,
} from '../agents/claude/plugin-installer.js';
